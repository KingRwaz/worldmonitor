interface:
  display_name: "Worldmonitor"
  short_description: "Repo-specific patterns and workflows for worldmonitor"
  default_prompt: "Use the worldmonitor repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true